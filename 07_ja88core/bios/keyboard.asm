; ----------------------------------------------------------------------
; Обработка прерывания IRQ#1
; ----------------------------------------------------------------------

int9:
            push    es
            mov     es, [cs:SEG_B800h]
            in      al, $60
            mov     [es:0], ax
            mov     al, $20
            out     $20, al
            pop     es
            iret
